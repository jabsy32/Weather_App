{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true, // Required for referenced projects
    "outDir": "./dist",
    "noEmit": true, // Allow emitting files
    "emitDeclarationOnly": false // Emit both JS and declaration files
  },
  "include": ["src"]
}